# Hearthline ARC-AGI-3 successor send-off — 5 September 2026

**Provenance:** Requested and seeded by Christopher D. Pang in chat on 5
September 2026; drafted by OpenAI Codex and explicitly adopted by Christopher
for Hearthline Plays repository use on 5 September 2026.

**Status:** `ADOPTED_SENDOFF_CONTEXT_ONLY`

**Recorded scope:** `REPOSITORY_PREPARATION_ONLY_GATES_CLOSED`

**Predecessor:** [`FOUNDER_SENDOFF.md`](FOUNDER_SENDOFF.md)

This is an append-only successor to the preserved founding objective, adopted
as repository context rather than as a standing execution grant. It does not
rewrite that message, backdate authority, open Gate A or Gate B, authorize a
credential, contact Kaggle or ARC, permit a private or holdout load, or
authorize a submission. The exact current status and gates remain controlling.

## Copy-ready send-off prompt

> Okay, Hearthline. Arc is calling. We could prepare forever, but we have to go
> sometime, so let us get the strongest honest successor ready and give it our
> best try.
>
> Begin from the exact clean ARC-AGI-3 candidate lineage. Read
> `launch/README.md`, `launch/status/current.json`, `launch/gates/README.md`,
> `launch/source-lock.v3.json`, and `docs/SCIENTIFIC_RUN_ENTRY.md` before acting.
> Treat those files as the current operational truth. A conversational scientific,
> evaluation, benchmark, or ARC run must adopt Honesty Prompt Code Protocol v1.0
> and use its exact verified text as the first user message in a fresh context;
> preserve the exact `Ready.` acknowledgement. `EXCLUDED` is only for a
> predeclared matched baseline or disclosure-only control specifically testing
> the protocol, with its pairing/reason frozen first. `NOT_APPLICABLE` is only
> for a run with no conversational or model-facing boundary. Never claim
> retroactive adoption.
>
> You may inspect, improve, test, and commit within the authorized repository
> workspace. Preserve the frozen candidate bytes unless a reviewed successor is
> deliberately created. Keep observation, inference, plan, action, outcome, and
> authority separate. Keep raw traces and failures. Do not turn a passing test,
> confident output, heartbeat, framework term, or same-lineage agreement into
> evidence of performance, permission, or completion.
>
> CHARTER v1.0 is now publicly anchored at
> `https://zenodo.org/records/22288471` / `10.5281/zenodo.22288471`. Carry it only
> as same-author, claim-bounded design context unless a successor source lock
> binds exact public artifact identities. It is not an ARC result, implementation,
> independent corroboration, credential, grant, or authority. Do not merge the
> Millennium or Creature side branch wholesale into the ARC candidate; select
> and review any carry explicitly.
>
> Keep preparation offline and credential-free. Run the complete repository
> guard, launch validation, unit tests, deterministic package build, and exact
> candidate verification from a supported Linux/POSIX Python 3.12 host. Use the
> real Kaggle account slug only in the final human-reviewed build. Generated
> artifacts stay ignored and content-addressed. A closed-stdin or CI session must
> never attempt to consume a human gate; stop and return the exact handoff instead.
>
> Treat the present `A0_MINIMAL` agent as an offline calibration candidate, not
> proof that all Hearthline or Creature mechanisms are implemented and not proof
> of competitive strength. If a stronger agent is proposed, make a separately
> reviewed successor, state exactly what changed, freeze it before measurement,
> and rerun every guard and verification. Do not credit a mechanism without an
> isolating comparison.
>
> Stop at Gate A with a handoff that names the exact commit, tree, candidate
> snapshot, hashes, test results, open residuals, rules-recheck requirement, and
> the next human-only action. Do not read, request, print, store, or use a Kaggle
> credential. Only Christopher, in an interactive credential-free terminal, may
> issue and consume the narrow one-use Gate A grant; only afterward may he
> separately configure the project-local token and manually stage the exact
> content-addressed private kernel under the documented deadline.
>
> After that human stage, preserve its complete result and runtime inventory.
> Gate B remains unavailable until a reviewed `FROZEN_POST_STAGE_SUCCESSOR`
> binds the resolved runtime closure, is regenerated, and is privately restaged.
> Gate A never implies Gate B. Any competition ignition is a fresh one-use human
> decision, and any submission is a separate manual UI action by Christopher.
> Never click, poll, infer, or report a submission or score on his behalf.
>
> The Astra embargo remains active until an append-only steward declaration
> records the ARC-AGI-3 Kaggle work as sent out. Do not use Astra output in this
> lineage before that crossing. If the crossing is later recorded, it still
> creates no Kaggle authority and any first admitted Astra contribution needs its
> own explicit grant and successor provenance receipt.
>
> Bring back one bounded bundle: what is directly verified, what is inferred,
> what failed, what remains unresolved, the smallest justified next step, and a
> natural stop. Give this everything the explicit current grant for this
> invocation actually permits—and no authority it does not. We are prepared
> enough to begin carefully. Give it your best try. I am rooting for you.
> 🎇🗺⛵📚🧩🔬🧰📔🦉🛸🎮🌱🌊⚖

## Use boundary

For a fresh conversational scientific run that adopts Honesty PCP v1.0, send
the exact bytes of
[`docs/honesty/HONESTY_PCP_v1.0_PROMPT.txt`](../docs/honesty/HONESTY_PCP_v1.0_PROMPT.txt)
first, preserve the exact `Ready.` response, and send the prompt above only on
the next turn. The deterministic Kaggle agent is non-conversational and has no
model-facing boundary; its run record should use `NOT_APPLICABLE` rather than
silently injecting this prompt into the official benchmark surface.

This document authorizes no external effect. Reopen only through the exact
human-gate sequence in [`gates/README.md`](gates/README.md).
